import {StyleSheet} from "react-native";

export const Colors = {
    white: '#fff',
    red: '#ff4348',
    playerColor: '#6cb936',
    computerColor: '#312fff',
    lightGray: '#EEE',
    veryLightGray: '#FAFAFA'
};

export const Theme = StyleSheet.create({
    error: {
        color: Colors.red
    }
});
